import React from "react";
import { RouteComponentProps } from "react-router-dom";

const SettingsPresenter: React.SFC<RouteComponentProps<any>> = () => (
  <span>SettingsPresenter</span>
);

export default SettingsPresenter;
