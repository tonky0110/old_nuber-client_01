import React from "react";
import { RouteComponentProps } from "react-router-dom";

const PhoneLoginPresenter: React.SFC<RouteComponentProps<any>> = () => (
  <span>PhoneLoginPresenter</span>
);

export default PhoneLoginPresenter;
