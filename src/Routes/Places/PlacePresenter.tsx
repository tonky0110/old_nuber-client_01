import React from "react";
import { RouteComponentProps } from "react-router-dom";

const PlacesPresenter: React.SFC<RouteComponentProps<any>> = () => (
  <span>PlacesPresenter</span>
);

export default PlacesPresenter;
