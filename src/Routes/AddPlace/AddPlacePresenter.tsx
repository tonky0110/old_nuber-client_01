import React from "react";
import { RouteComponentProps } from "react-router-dom";

const AddPlacePresenter: React.SFC<RouteComponentProps<any>> = () => (
  <span>AddPlacePresenter</span>
);

export default AddPlacePresenter;
