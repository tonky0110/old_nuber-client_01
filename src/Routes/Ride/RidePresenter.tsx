import React from "react";
import { RouteComponentProps } from "react-router-dom";

const RidePresenter: React.SFC<RouteComponentProps<any>> = () => (
  <span>RidePresenter</span>
);

export default RidePresenter;
