import React from "react";
import { RouteComponentProps } from "react-router-dom";

const SocialLoginPresenter: React.SFC<RouteComponentProps<any>> = () => (
  <span>SocialLoginPresenter</span>
);

export default SocialLoginPresenter;
